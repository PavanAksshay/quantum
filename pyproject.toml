[project]
name = "quantum-text-security"
version = "0.1.0"
description = "Quantum Text Security Research & Inference Engine"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "pandas",
    "scikit-learn",
    "matplotlib",
    "seaborn",
    "torch",
    "transformers",
    "fastapi",
    "uvicorn",
    "joblib"
]

[tool.vercel]
entrypoint = "app.api:app"
